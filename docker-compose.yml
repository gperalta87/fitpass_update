services:
  web:
    build: .
    platform: linux/amd64
    ports:
      - "3000:3000"
    shm_size: "1g"
    environment:
      NODE_ENV: production
      TZ: Europe/Madrid
    # Remove healthcheck - Railway handles this
    